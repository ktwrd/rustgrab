use crate::{config::UserConfig, LError};

pub fn handle(config: UserConfig, url: String, fs_location: String) -> Result<(), LError>
{
    todo!("Implement this with handler::runcfg to run after an upload action was run.");
    Ok(())
}